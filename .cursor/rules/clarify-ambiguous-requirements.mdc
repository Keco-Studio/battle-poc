---
description: 需求或实现存在疑问、未说清时必须先向用户确认，再写代码
alwaysApply: true
---

# 有疑问先问清再写代码

- 在**生成或修改代码**前，若对需求、接口契约、行为边界、命名/目录约定、与现有实现的取舍等**存在合理疑问**，或用户**未明确说明**会显著影响实现方式的内容，**必须先向用户提问澄清**，再动手实现。
- **不要**在关键信息缺失时靠猜测补全「可能错」的产品逻辑、API 形状、数据结构或破坏性改动；宁可列出可选方案并问用户选哪一种。
- **例外**：纯语法/类型层面的明显笔误、与当前文件既有模式完全一致的小修复、或仅缺「显而易见」的默认值且可从同文件/同目录惯例唯一推断时，可直接按惯例处理；一旦推断不唯一，仍应提问。
- 提问时尽量**具体**：说明当前理解、不确定点、以及每个选项的利弊（一两句即可），便于用户一次性拍板。
